<div id="<%= dom_id category %>" class= "d-flex align-items-start justify-content-between" >
  <h4><%= link_to category.name, category_discussions_path(category) %></h4>
  <%= discussion_count(category) %>
</div>
